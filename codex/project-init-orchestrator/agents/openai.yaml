interface:
  display_name: "Project Init Orchestrator"
  short_description: "Executable Codex project startup and guardrails"
  default_prompt: "Use $project-init-orchestrator to inspect and initialize this project, clarify the design, create bounded roles, and continue through verified completion."
